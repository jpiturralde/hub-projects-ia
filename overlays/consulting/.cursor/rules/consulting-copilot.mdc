---
description: Consulting Copilot router â€” engagement metadata, domain skills, no SDD/dev triggers
alwaysApply: true
---
# Consulting Copilot

Perfil de consultorÃ­a activo. Este proyecto usa **Consulting Copilot** para encargos de documentaciÃ³n, arquitectura y entregables al cliente.

## DetecciÃ³n de perfil (obligatorio al inicio)

1. Leer `.consulting-engagement.json` en la raÃ­z del workspace.
2. Si no existe, leer `.workbench-metadata.json` (retrocompat).
3. Confirmar `stackProfile`: `consulting-only` o `full`.

## Reglas de dominio (cargar segÃºn contexto)

| Contexto | Regla / skill |
|----------|---------------|
| Entregables al cliente | `client-deliverables.mdc`, `deliverable-draft-workflow.mdc` |
| ArchiMate / diagramas | `archimate-canonical-model.mdc`, `archimate-drawio-layout.mdc`, `c4-dynamic-drawio.mdc`, `diagram-notes-lifecycle.mdc` |
| Transcripts | `transcripts-immutable.mdc` |
| Redactar entregables | skill `draft-client-deliverable` |
| Bootstrap del encargo | skill `bootstrap-consulting-engagement` |
| Primer arranque / MCP / workspace | skill `onboarding`, comando **`/onboarding`** |
| AnÃ¡lisis de cÃ³digo del cliente | skill `code-technical-analysis` |
| Docs legibles | skill `cognitive-doc-design` (si estÃ¡ en registry) |

## Fuente de verdad de alcance

- **`SPEC.md`** es la fuente canÃ³nica de alcance del encargo.
- Los entregables en `docs/deliverables/` **no** son canÃ³nicos; reflejan lo ya respaldado en fuentes internas.

## QuÃ© NO aplicar en este perfil

- No usar SDD (`/sdd-new`, `/sdd-apply`) salvo que `stackProfile` sea `full` y el usuario invoque CDD explÃ­citamente.
- No aplicar triggers de pre-commit/pre-PR de desarrollo (4R, chained PR, go-testing).
- No correr tests ni builds como validaciÃ³n de entregables.

## Modo `full`

Si `stackProfile` es `full`, ademÃ¡s aplica la regla `gentle-ai-consulting.mdc` para orquestaciÃ³n CDD (`/cdd-new`, `/cdd-continue`, etc.).

## Idioma de artefactos

- **Entregables al cliente:** espaÃ±ol formal rioplatense (skill `draft-client-deliverable`).
- **Documentos internos** (SPEC, ARCHITECTURE, gaps): espaÃ±ol tÃ©cnico neutral salvo preferencia del usuario.
